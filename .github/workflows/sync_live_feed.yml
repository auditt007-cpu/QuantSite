# Disabled: live_feed is published on-VPS to /var/www/html (same-origin HTTPS).
# Do not use GitHub Actions as a data relay (Mixed Content / stale Pages copy).
#
# To re-enable accidentally, delete this file; the live path is VPS nginx only.

name: Sync Live Feed (disabled)

on:
  workflow_dispatch:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Disabled
        run: |
          echo "Sync Live Feed workflow is disabled."
          echo "VPS tg_engine publishes /var/www/html/live_feed.json every 5s."
          exit 0
